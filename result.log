int elementX = element.getLocation().getX();
        int elementY = element.getLocation().getY();

        // Use Actions class to move to the element and click using the coordinates
        Actions actions = new Actions(driver);
        actions.moveByOffset(elementX, elementY).click().perform();
